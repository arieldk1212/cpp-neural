// set of unique objects, only once in a set.
// unordered set is faster.
// good to handle duplicates of data.

#include <set>
#include <unordered_set>

int main () {
  std::set<int> container;
  // container.insert();

  // std::unordered_set()  by not worrying the order, we can get optimized functions.
}