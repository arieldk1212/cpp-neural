// can push back or front of the list.
// it creates a node, they are joined together with ptrs.
// iterating until thers no next node.
// no index, cannot access like this - [i]

#include <list>

int main () {

  std::list<int> container;

  return 0;
}