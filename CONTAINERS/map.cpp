// begin, end
// corrolation between key, value.
// can iterate thru pair -> [key, value] and print them inside the for.

#include <string>
#include <map>
#include <unordered_map>

int main () {
  std::map<std::string, int> container;
  std::unordered_map<std::string, int> container2;

}